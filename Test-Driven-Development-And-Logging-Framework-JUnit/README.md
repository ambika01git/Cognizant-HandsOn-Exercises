# Test Driven Development and Logging Framework - JUnit

## Exercise 1: Setting Up JUnit

* Created a Maven project.
* Added the JUnit dependency in `pom.xml`.
* Created a test class for unit testing.

## Exercise 2: Writing Basic JUnit Tests

* Created a `Calculator` class.
* Wrote JUnit test cases for the `add()` and `subtract()` methods.

## Exercise 3: Assertions in JUnit

* Used `assertEquals()`
* Used `assertTrue()`
* Used `assertFalse()`
* Used `assertNull()`
* Used `assertNotNull()`

## Exercise 4: Arrange-Act-Assert (AAA) Pattern

* Implemented the Arrange-Act-Assert (AAA) pattern.
* Used `@BeforeEach` for test setup.
* Used `@AfterEach` for test teardown.

## Technologies Used

* Java
* Maven
* JUnit 5
* Eclipse IDE
